name: NaviTune
options:
  bundleIdPrefix: com.d0vere
  deploymentTarget:
    iOS: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.0"
    CODE_SIGN_STYLE: Automatic
targets:
  NavidromeMusicSync:
    type: application
    platform: iOS
    sources:
      - path: NavidromeMusicSync
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.d0vere.NaviTune
        PRODUCT_NAME: NaviTune
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: NaviTune
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.music
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_BGTaskSchedulerPermittedIdentifiers: com.d0vere.NaviTune.continued-sync
        INFOPLIST_KEY_UIBackgroundModes: processing
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
        CODE_SIGNING_ALLOWED: NO
        CODE_SIGNING_REQUIRED: NO
        SWIFT_OBJC_BRIDGING_HEADER: NavidromeMusicSync/BridgingHeader.h
        HEADER_SEARCH_PATHS:
          - "$(SRCROOT)/Vendor/idevice"
        LIBRARY_SEARCH_PATHS:
          - "$(SRCROOT)/Vendor/idevice"
        OTHER_LDFLAGS:
          - "$(inherited)"
          - "-lidevice_ffi"
          - "-lsqlite3"
